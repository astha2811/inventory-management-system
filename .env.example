# PostgreSQL credentials — change before deploying
POSTGRES_USER=postgres
POSTGRES_PASSWORD=astha
POSTGRES_DB=inventory_db

# Backend (used by backend container)
# DATABASE_URL=postgresql://postgres:astha@db:5432/inventory_db
DATABASE_URL=postgresql://postgres:astha@localhost:5432/inventory_db


# Frontend (set to your deployed backend URL in production)
VITE_API_URL=http://localhost:8000
